# Backend

最小可用後端，提供：

- `POST /auth/register`
- `POST /auth/login`
- `GET /me`
- `GET /favorites`
- `POST /favorites/:gameId`
- `DELETE /favorites/:gameId`

啟動：

```bash
cd backend
npm start
```

預設監聽 `http://localhost:4000`
